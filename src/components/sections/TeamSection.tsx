import { Award, BriefcaseBusiness } from "lucide-react";
import { Avatar, AvatarFallback, AvatarImage } from "@/components/ui/avatar";
import { Badge } from "@/components/ui/badge";
import { Card, CardContent } from "@/components/ui/card";
import { HoverCard, HoverCardContent, HoverCardTrigger } from "@/components/ui/hover-card";
import { team } from "@/data/siteData";

export function TeamSection() { return <section id="team" className="py-20"><div className="container px-4"><div className="mx-auto mb-10 max-w-2xl text-center"><Badge variant="outline">Команда</Badge><h2 className="mt-4 text-3xl font-bold sm:text-4xl">Инженеры, которым можно доверять</h2><p className="mt-3 text-muted-foreground">Опыт, аккуратность и любовь к технике объединяют наших мастеров.</p></div><div className="grid gap-5 sm:grid-cols-2 lg:grid-cols-4">{team.map(([name, role, experience, image]) => <HoverCard key={name}><HoverCardTrigger asChild><Card className="group cursor-pointer overflow-hidden transition hover:-translate-y-1 hover:shadow-lg"><img src={image} alt={name} className="aspect-square w-full object-cover grayscale transition duration-500 group-hover:grayscale-0" loading="lazy" /><CardContent className="p-4"><h3 className="font-semibold">{name}</h3><p className="text-sm text-primary">{role}</p><p className="mt-2 text-xs text-muted-foreground">{experience}</p></CardContent></Card></HoverCardTrigger><HoverCardContent><div className="flex items-center gap-3"><Avatar><AvatarImage src={image} /><AvatarFallback>{name[0]}</AvatarFallback></Avatar><div><p className="font-semibold">{name}</p><p className="text-sm text-muted-foreground">Старший специалист</p></div></div><p className="mt-3 flex gap-2 text-sm text-muted-foreground"><Award className="h-4 w-4 text-primary" />Аттестован по ремонту техники</p></HoverCardContent></HoverCard>)}</div><div className="mx-auto mt-10 flex max-w-xl items-center justify-center gap-3 rounded-xl border bg-muted/30 p-4 text-center text-sm text-muted-foreground"><BriefcaseBusiness className="h-5 w-5 text-primary" />Все инженеры регулярно повышают квалификацию.</div></div></section>; }
