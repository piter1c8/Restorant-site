import { useState } from "react";
import { CalendarDays, CircleCheck as CheckCircle2, Hop as Home, MapPin, Video, Building2 } from "lucide-react";
import { Badge } from "@/components/ui/badge";
import { Button } from "@/components/ui/button";
import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card";
import { Calendar } from "@/components/ui/calendar";
import { Input } from "@/components/ui/input";
import { Label } from "@/components/ui/label";
import { RadioGroup, RadioGroupItem } from "@/components/ui/radio-group";
import { Select, SelectContent, SelectItem, SelectTrigger, SelectValue } from "@/components/ui/select";
import { toast } from "@/hooks/use-toast";
import { bookingTypes, deviceTypes, timeSlots } from "@/data/siteData";

const typeIcons = { "В сервисном центре": Building2, "Выезд мастера на дом": Home, "Удалённая консультация": Video };
export function BookingSection() { const [date, setDate] = useState<Date | undefined>(new Date()); const [time, setTime] = useState(""); const [name, setName] = useState(""); const [device, setDevice] = useState(""); const [type, setType] = useState(bookingTypes[0]); const [done, setDone] = useState(false); const submit = (event: React.FormEvent<HTMLFormElement>) => { event.preventDefault(); if (!date || !time || !name || !device) return; setDone(true); toast({ title: "Заявка принята", description: "Мы перезвоним вам в течение 15 минут." }); }; return <section id="booking" className="relative overflow-hidden py-20"><div className="absolute inset-0 bg-grid opacity-20" /><div className="container relative px-4"><div className="mx-auto mb-10 max-w-2xl text-center"><Badge variant="outline">Онлайн-запись</Badge><h2 className="mt-4 text-3xl font-bold sm:text-4xl">Забронируйте удобное время</h2><p className="mt-3 text-muted-foreground">Выберите дату и свободный слот — мастер будет ждать вас без очереди.</p></div>{done ? <Card className="mx-auto max-w-xl border-green-200 bg-green-50 dark:border-green-900 dark:bg-green-950/30"><CardContent className="flex flex-col items-center p-10 text-center"><CheckCircle2 className="h-14 w-14 text-green-600" /><h3 className="mt-4 text-xl font-bold">Вы записаны на ремонт</h3><p className="mt-2 text-muted-foreground">Заявка на {date?.toLocaleDateString("ru-RU")} в {time} отправлена. Мы скоро свяжемся с вами.</p><Button variant="outline" className="mt-6" onClick={() => setDone(false)}>Изменить запись</Button></CardContent></Card> : <form onSubmit={submit} className="mx-auto grid max-w-5xl gap-6 lg:grid-cols-[1fr_1fr_.85fr]"><Card><CardHeader><CardTitle className="flex items-center gap-2 text-lg"><CalendarDays className="h-5 w-5 text-primary" />Дата визита</CardTitle></CardHeader><CardContent className="flex justify-center p-2 pt-0"><Calendar mode="single" selected={date} onSelect={setDate} disabled={(day) => day < new Date(new Date().setHours(0, 0, 0, 0))} /></CardContent></Card><Card><CardHeader><CardTitle className="text-lg">Свободное время</CardTitle></CardHeader><CardContent><div className="grid grid-cols-3 gap-2">{timeSlots.map((slot) => <Button key={slot} type="button" size="sm" variant={time === slot ? "default" : "outline"} onClick={() => setTime(slot)}>{slot}</Button>)}</div><div className="mt-6"><Label>Формат обращения</Label><RadioGroup value={type} onValueChange={setType} className="mt-2 gap-2">{bookingTypes.map((item) => { const Icon = typeIcons[item as keyof typeof typeIcons]; return <Label key={item} className={`flex cursor-pointer items-center gap-2 rounded-lg border p-2 text-xs ${type === item ? "border-primary bg-primary/5" : ""}`}><RadioGroupItem value={item} /><Icon className="h-4 w-4 text-primary" />{item}</Label>; })}</RadioGroup></div></CardContent></Card><Card><CardHeader><CardTitle className="text-lg">О вас</CardTitle></CardHeader><CardContent className="space-y-4"><div className="space-y-2"><Label htmlFor="book-name">Ваше имя</Label><Input id="book-name" value={name} onChange={(event) => setName(event.target.value)} placeholder="Иван Иванов" required /></div><div className="space-y-2"><Label>Устройство</Label><Select value={device} onValueChange={setDevice} required><SelectTrigger><SelectValue placeholder="Выберите тип" /></SelectTrigger><SelectContent>{deviceTypes.map((item) => <SelectItem key={item} value={item}>{item}</SelectItem>)}</SelectContent></Select></div><div className="space-y-2"><Label htmlFor="book-phone">Телефон</Label><Input id="book-phone" type="tel" placeholder="+7 (___) ___-__-__" required /></div><Button type="submit" className="w-full"><MapPin className="h-4 w-4" />Забронировать слот</Button><p className="text-center text-xs text-muted-foreground">Диагностика бесплатно при ремонте</p></CardContent></Card></form>}</div></section>; }
