import { useState } from "react";
import { Mail, MapPin, MessageCircle, Phone, Send } from "lucide-react";
import { Badge } from "@/components/ui/badge";
import { Button } from "@/components/ui/button";
import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card";
import { Input } from "@/components/ui/input";
import { Label } from "@/components/ui/label";
import { Textarea } from "@/components/ui/textarea";
import { toast } from "@/hooks/use-toast";
import { siteConfig } from "@/data/siteData";

export function ContactsSection() { const [sent, setSent] = useState(false); const submit = (event: React.FormEvent<HTMLFormElement>) => { event.preventDefault(); setSent(true); toast({ title: "Сообщение отправлено", description: "Мы свяжемся с вами в ближайшее время." }); }; return <section id="contacts" className="bg-muted/30 py-20"><div className="container grid gap-10 px-4 lg:grid-cols-[.8fr_1.2fr] lg:items-start"><div><Badge variant="outline">Контакты</Badge><h2 className="mt-4 text-3xl font-bold sm:text-4xl">Расскажите, что случилось</h2><p className="mt-4 text-muted-foreground">Опишите проблему — специалист подскажет решение и сориентирует по стоимости.</p><div className="mt-8 grid gap-4"><a href={siteConfig.phoneHref} className="flex items-center gap-3 rounded-xl border bg-card p-4 transition hover:border-primary"><Phone className="h-5 w-5 text-primary" /><span><small className="block text-muted-foreground">Телефон</small><strong>{siteConfig.phone}</strong></span></a><a href={`mailto:${siteConfig.email}`} className="flex items-center gap-3 rounded-xl border bg-card p-4 transition hover:border-primary"><Mail className="h-5 w-5 text-primary" /><span><small className="block text-muted-foreground">Почта</small><strong>{siteConfig.email}</strong></span></a><div className="flex items-center gap-3 rounded-xl border bg-card p-4"><MapPin className="h-5 w-5 text-primary" /><span><small className="block text-muted-foreground">Адрес</small><strong>{siteConfig.address}</strong></span></div></div><div className="mt-5 flex gap-2"><Button variant="outline" asChild><a href={siteConfig.telegram}><Send className="h-4 w-4" />Telegram</a></Button><Button variant="outline" asChild><a href={siteConfig.whatsapp}><MessageCircle className="h-4 w-4" />WhatsApp</a></Button></div></div><Card><CardHeader><CardTitle>Обратная связь</CardTitle></CardHeader><CardContent>{sent ? <div className="flex min-h-[300px] flex-col items-center justify-center text-center"><MessageCircle className="h-12 w-12 text-green-600" /><h3 className="mt-4 text-xl font-semibold">Спасибо за обращение</h3><p className="mt-2 text-muted-foreground">Мастер свяжется с вами по указанному телефону.</p><Button variant="outline" className="mt-6" onClick={() => setSent(false)}>Отправить ещё</Button></div> : <form onSubmit={submit} className="grid gap-4"><div className="grid gap-4 sm:grid-cols-2"><div className="space-y-2"><Label htmlFor="contact-name">Имя</Label><Input id="contact-name" placeholder="Иван Иванов" required /></div><div className="space-y-2"><Label htmlFor="contact-phone">Телефон</Label><Input id="contact-phone" type="tel" placeholder="+7 (___) ___-__-__" required /></div></div><div className="space-y-2"><Label htmlFor="contact-email">Email</Label><Input id="contact-email" type="email" placeholder="ivan@example.ru" /></div><div className="space-y-2"><Label htmlFor="contact-message">Опишите проблему</Label><Textarea id="contact-message" placeholder="Например: ноутбук не включается после залития..." className="min-h-32" required /></div><Button type="submit" className="w-full"><Send className="h-4 w-4" />Отправить заявку</Button><p className="text-center text-xs text-muted-foreground">Нажимая кнопку, вы соглашаетесь с политикой конфиденциальности.</p></form>}</CardContent></Card></div></section>; }
