import { useState } from "react";
import { Images, Maximize2 } from "lucide-react";
import { Badge } from "@/components/ui/badge";
import { Dialog, DialogContent, DialogTitle } from "@/components/ui/dialog";
import { gallery } from "@/data/siteData";

export function GallerySection() { const [selected, setSelected] = useState<(typeof gallery)[number] | null>(null); return <section id="gallery" className="bg-muted/30 py-20"><div className="container px-4"><div className="mb-10 flex items-end justify-between gap-4"><div><Badge variant="outline">Наша работа</Badge><h2 className="mt-4 text-3xl font-bold sm:text-4xl">Здесь техника получает вторую жизнь</h2><p className="mt-3 max-w-xl text-muted-foreground">Чистая лаборатория, оборудование и внимание к каждой детали.</p></div><Images className="hidden h-10 w-10 text-primary/30 sm:block" /></div><div className="grid auto-rows-[180px] gap-3 sm:grid-cols-2 lg:grid-cols-4">{gallery.map(([src, title, type], index) => <button type="button" key={title} onClick={() => setSelected([src, title, type])} className={`group relative overflow-hidden rounded-xl text-left ${index === 0 ? "sm:col-span-2 sm:row-span-2" : index === 3 ? "lg:col-span-2" : ""}`}><img src={src} alt={title} className="h-full w-full object-cover transition duration-500 group-hover:scale-105" loading="lazy" /><span className="absolute inset-0 bg-gradient-to-t from-slate-950/80 to-transparent" /><span className="absolute inset-x-4 bottom-4 flex items-end justify-between text-white"><span><small className="block text-white/70">{type}</small><strong>{title}</strong></span><Maximize2 className="h-4 w-4 opacity-0 transition group-hover:opacity-100" /></span></button>)}</div></div><Dialog open={Boolean(selected)} onOpenChange={(open) => !open && setSelected(null)}><DialogContent className="max-w-4xl p-2"><DialogTitle className="sr-only">{selected?.[1]}</DialogTitle>{selected && <img src={selected[0]} alt={selected[1]} className="max-h-[75vh] w-full rounded-lg object-contain" />}</DialogContent></Dialog></section>; }
