import { ArrowRight, BadgeCheck, ClipboardCheck, PhoneCall, Stethoscope, Wrench } from "lucide-react";
import { Badge } from "@/components/ui/badge";
import { Card, CardContent } from "@/components/ui/card";
import { processSteps } from "@/data/siteData";

const icons = { PhoneCall, Stethoscope, Wrench, ClipboardCheck, BadgeCheck };
export function ProcessSection() { return <section id="process" className="py-20"><div className="container px-4"><div className="mx-auto mb-12 max-w-2xl text-center"><Badge variant="outline">Как всё проходит</Badge><h2 className="mt-4 text-3xl font-bold sm:text-4xl">От заявки до исправной техники — 5 шагов</h2><p className="mt-3 text-muted-foreground">Никаких сложных терминов и неожиданных решений.</p></div><div className="grid gap-5 md:grid-cols-5">{processSteps.map(([number, title, description, icon], index) => { const Icon = icons[icon as keyof typeof icons]; return <div key={number} className="relative"><Card className="h-full transition hover:-translate-y-1 hover:shadow-md"><CardContent className="p-5"><div className="flex items-center justify-between"><span className="text-4xl font-bold text-primary/15">{number}</span><span className="flex h-11 w-11 items-center justify-center rounded-xl bg-primary/10 text-primary"><Icon className="h-5 w-5" /></span></div><h3 className="mt-5 font-semibold">{title}</h3><p className="mt-2 text-sm leading-relaxed text-muted-foreground">{description}</p></CardContent></Card>{index < processSteps.length - 1 && <ArrowRight className="absolute -right-4 top-1/2 hidden h-5 w-5 -translate-y-1/2 text-primary md:block" />}</div>; })}</div></div></section>; }
